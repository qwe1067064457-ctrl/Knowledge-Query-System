# V2 Migration Candidates 20260518

这个目录保存 `V2` 迁移过程中，按当前规则筛出的**建议人工复核候选样本**。

## 文件组成

- `*.jsonl`
  - 各数据集的候选样本列表
- `summary.json`
  - 每个数据集选中了多少条

## 当前用途

- 给人工 review 排优先级
- 不直接作为训练集
- 不直接覆盖原始 `gold/silver/frozen` 文件

## 当前判断

如果下一轮要从 `V2 auto` 进入更稳定的训练线，这个目录优先用于：

1. 挑人工复核样本
2. 生成更干净的 `V2 reviewed` 子集
3. 再决定哪些 auto 标签可以正式进入训练
